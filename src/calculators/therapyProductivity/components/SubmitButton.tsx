
import React from 'react';
import { Button } from '@/components/ui/button';

const SubmitButton: React.FC = () => {
  return (
    <Button type="submit" className="w-full">Calculate Productivity</Button>
  );
};

export default SubmitButton;
